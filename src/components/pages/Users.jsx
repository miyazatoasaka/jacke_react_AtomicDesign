import styled from "styled-components";
export const Users = () => {
  return (
    <SContainer>
      <h2>ユーザー一覧ページです</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
